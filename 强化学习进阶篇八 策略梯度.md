# 简介

 Q-learning、DQN 及 DQN 改进算法都是**基于价值**的方法。其中 Q-learning 是处理**有限状态**的算法，而 DQN 可以用来解决**连续状态**的问题。除了基于值函数的方法，还有一支非常经典的方法，那就是**基于策略**的方法。在此之前要学一些基础：

- [深度学习-交叉熵](https://blog.csdn.net/qq00769539/article/details/163107459?spm=1011.2124.3001.6209)  最大似然估计 MLE

- **梯度**  
   $f(x)=x^2+2$ 这是开口向上的抛物线,
  
  1. 一阶导数 (**梯度**):$f'(x)=2x$ 变化率、切线斜率 
     
     - $x<0，x递增$  $f'(x)=2x <0 $  也是在递增，这叫梯度上升。原$f(x)=x^2+2$是递减
     - $0<x，x递时$ $f'(x)=2x >0 $ 也是在递增，原$f(x)=x^2+2$是递增
  
  2. 二阶导数: $f''(x)=2$ 加速度信息   
     
     - $f''(x)=2>0$ 极小值点
     - 若 $f''(x)<0$ 极大值点
       
       



# 策略梯度核心思想



**回顾价值函数：**

**状态价值函数**  $V^{\pi}(s)=\mathbb{E}_{\pi}[G_t|S_t=s]= \sum_{a'\in \mathcal{A}} \pi(s,a) Q^{\pi}(s,a)$

**动作价值函数**  $Q^{\pi}(s,a)= \mathbb{E}_{\pi}[G_t|A_t=a,S_t=s]=r(s,a)+\gamma \sum p(s'|s,a) V^{\pi}(s')$

其中$\pi(s,a)$ 策略下动作$a$的条件概率，寻找最优参数  $\boldsymbol{\theta}$ 这是策略学习的目标，和**最大似然估计**核心思想是相同的都是在寻找最优参数 $\boldsymbol{\theta}$。详情见[深度学习.交叉熵 --> 最大似然估计 MLE](https://blog.csdn.net/qq00769539/article/details/163107459?spm=1011.2124.3001.6209)，将策略学习的目标函数定义为：

$J(\theta)=\mathbb{E}_{\pi}[V^{\pi}(s)]=\sum_{a'\in \mathcal{A}} \underbrace{\pi(s,a)}_\theta \underbrace{[r(s,a)+\gamma \sum p(s'|s,a) V^{\pi}(s')]}_{G_t=Q^{\pi}(s,a)}$  

简化一下公式：$J(\theta)=\sum \pi(s,a)*Q^{\pi}(s,a)$ 其中$\pi(s,a)$是概率函数： $p(x;\theta)=\pi(s,a)$

可以用一个神经网络模型来为这样一个策略函数建模输出动作概率分布$p(x;\theta)$。

$J(\theta)$目标函数用**对数似然函数** $J(\theta)=\sum_{i=1}^m G_i * \ln \pi_\theta(a_i|s)$

最后对$J(\theta)$求导 $\nabla J(\theta)= \sum_{i=1}^m G_i* \nabla \ln \pi_\theta(a_i|s)$ 

**策略梯度公式** $\nabla J(\theta)= \sum_{i=1}^m G_i* \nabla \ln \pi_\theta(a_i|s)$

## 实例

动作空间：$[a_1,a_2]$，状态空间：$[s_0,s_1,s_2]$

采样轨迹: $[(s_0,a_1,r=1),(s_1,a_2,r=1),(s_2,a_2,r=0)]$

折扣系数 $\gamma=1$

回合长度 $(T=3)$，时间下标 $(t=0,1,2)$

回报$G_t$汇总：$G_t=\sum_{k=0}^{\infty}\gamma^{k} R_{t+k}=>\sum_{k=0} R_{t+k}$

- $G_2=r_2=0$

- $G_1=r_1+G_2=1+0=1$

- $G_0=r_0+G_1+G_2=1+1+0=2$

汇总表

| $t$ | $s_t$ | $a_t$ | $r_t$ | $G_t$ |
|:---:|:-----:|:-----:|:-----:|:-----:|
| 0   | $s_0$ | $a_1$ | 1     | 2     |
| 1   | $s_1$ | $a_2$ | 1     | 1     |
| 2   | $s_2$ | $a_2$ | 0     | 0     |

根据策略梯度公式 $\nabla J(\theta)= \sum_{i=1}^m G_i* \nabla \ln \pi_\theta(a_i|s) $

### 策略梯度计算过程

1. $t=0$，$G_0=2$， $s_0, a_1$： $2*\nabla \ln \pi_\theta(a_1|s_0)$

2. $t=1$，$G_1=1$， $s_1, a_2$： $1*\nabla \ln \pi_\theta(a_2|s_1)$

3. $t=2$，$G_2=0$， $s_2, a_2$： $0*\nabla \ln \pi_\theta(a_2|s_2)$

4. 根据策略梯度公式 $\nabla J(\theta)=   [ 2*\nabla \ln \pi_\theta(a_1|s_0) + 1*\nabla \ln \pi_\theta(a_2|s_1) + 0 ]$，最大化期望回报 $J(\theta)$，要用**梯度上升**，深度学习默认做**梯度下降** 所以乘以负号 $\nabla J(\theta)= - [ 2*\nabla \ln \pi_\theta(a_1|s_0) + 1*\nabla \ln \pi_\theta(a_2|s_1) + 0 ]$

## 

## 实现关键代码

```c++
 蒙特卡洛方法采集数据存放在 QwList& vList
```

```c++
计算轨迹策略梯度
 int length = vList.size()-1;
 double G = 0;
 adam.zero_grad();

 for (int i = length; 0 <= i; i--)
 {
     // s,a,r,
     auto [s, a, r, s1, d] = vList[i];
     auto s0 = VectorDoubleTensor(s);
     auto act = torch::tensor({ {a} }, torch::kInt);

     auto action = m_Qnet->forward(s0).gather(1, act);
     auto logprob = torch::log(action + 1e-8);
     G = m_dbGamma * G + r;
     auto lass = -logprob * G;
     lass.backward();

 }
 adam.step();
```




