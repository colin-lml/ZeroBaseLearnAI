# 信息熵基础

信息熵（Information Entropy）是信息论中的核心概念，用于量化**随机变量的不确定性**。

日常语言中常说“信息很多”或“信息很少”，但这类描述并不精确。香农通过概率分布定义了熵，使“不确定性”能够被量化：

- 事件越难预测，熵通常越高；
- 事件完全确定时，熵为 `0`；
- 熵描述的是随机变量在发生前的平均不确定性，而不是某一次事件发生后的信息量。

当对数以 `2` 为底时，单位是 **比特（bit）**。

---

# 自信息（信息量）

对于一个具体事件 `x`，其自信息定义为：

$I(x)=-\log_2 p(x)$

其中，$p(x)$ 表示事件 $x$ 发生的概率。

事件越罕见，发生时带来的“意外程度”越大，因此自信息也越大。

$\begin{cases}
p(x)=1.00 \Rightarrow I(x)=-\log_2(1.00)=0 \\
p(x)=0.50 \Rightarrow I(x)=-\log_2(0.50)=1 \\
p(x)=0.25 \Rightarrow I(x)=-\log_2(0.25)=2
\end{cases}$

可以这样理解：

- 必然发生的事件没有新信息，因此信息量为 `0`；
- 概率为 `1/2` 的事件发生时，提供 `1 bit` 信息；
- 概率为 `1/4` 的事件发生时，提供 `2 bit` 信息。

> 概率为 `0` 的事件不会实际发生。在熵的计算中，按约定令：
> 
> $0\log_2 0=0$

## 彩票示例

假设某种彩票的奖项概率如下：

| 奖级  | 理论概率         | 自信息      | 单注奖金   |
|:---:|:------------:|:--------:| ------:|
| 一等奖 | $1 / 2^{24}$ | $24$ bit | 1000 万 |
| 二等奖 | $1 / 2^{20}$ | $20$ bit | 500 万  |
| 三等奖 | $1 / 2^{15}$ | $15$ bit | 3000 元 |
| 四等奖 | $1 / 2^{11}$ | $11$ bit | 200 元  |
| 五等奖 | $1 / 2^7$    | $7$ bit  | 10 元   |
| 六等奖 | $1 / 2^4$    | $4$ bit  | 5 元    |

例如：

- 中一等奖的概率为 $2^{-24}$，自信息为 $24$ bit；
- 中二等奖的概率为 $2^{-20}$，自信息为 $20$ bit。

因此，自信息可以将“事件有多意外”量化：越罕见的奖项，中奖时的信息量越大。

---

# 信息熵（香农熵）

## 离散熵

随机变量 $X$ 的香农熵定义为其自信息的期望：

$H(X)=\mathbb{E}[I(X)]=-\sum_x p(x)\log_2 p(x)$

熵衡量的是随机变量在结果发生前的平均不确定性。

- $H(X)$ 越大，结果越难预测；
- $H(X)$ 越小，结果越确定；
- 如果 $X$ 的结果完全确定，则 $H(X)=0$。

## 彩票熵的计算

中奖概率为：

$\begin{aligned}
P_{\text{中奖}}
&=2^{-24}+2^{-20}+2^{-15}+2^{-11}+2^{-7}+2^{-4} \\
&=\frac{1+16+512+8192+131072+1048576}{2^{24}} \\
&=\frac{1188369}{2^{24}}
\end{aligned}$

因此，不中奖的概率为：

$\begin{aligned}
p_{\text{不中奖}}
&=1-P_{\text{中奖}} \\
&=\frac{2^{24}-1188369}{2^{24}} \\
&=\frac{15588847}{2^{24}} \\
&\approx 0.929168
\end{aligned}$

将“未中奖”也视为一个结果，则彩票结果的熵为：

$\begin{aligned}
H(X)
={}&24\cdot2^{-24}
+20\cdot2^{-20}
+15\cdot2^{-15} \\
&+11\cdot2^{-11}
+7\cdot2^{-7}
+4\cdot2^{-4} \\
&-\frac{15588847}{2^{24}}
\log_2\left(\frac{15588847}{2^{24}}\right) \\
\approx{}& 0.4090\text{ bit}
\end{aligned}$

这个结果说明：

- 不中奖的概率约为 `92.92%`，因此彩票结果整体具有较强的可预测性；
- 一等奖的自信息虽然高达 `24 bit`，但发生概率极低，对总熵的贡献很小；
- 六等奖的单次信息量较低，但概率相对较高，因此对总熵的贡献更大。

> 自信息描述某个具体结果的罕见程度；熵则是所有可能结果自信息的概率加权平均。

## 熵的最大值

若随机变量 $X$ 有 $n$ 个可能结果，且每个结果等概率出现：

$p(x_i)=\frac{1}{n}$

则熵达到最大值：

$H(X)=\log_2 n$

例如，公平硬币正反面等概率出现：

$H(X)=-0.5\log_2 0.5-0.5\log_2 0.5=1\text{ bit}$

而一枚总是正面朝上的硬币，其结果完全确定：

$H(X)=0$

---

# 微分熵

离散熵适用于离散随机变量，例如天气、类别标签和骰子点数。

对于连续随机变量，其对应概念是**微分熵（Differential Entropy）**。设连续随机变量 $X$ 的概率密度函数为 $p(x)$，则：

$h(X)=-\int_{-\infty}^{+\infty}p(x)\log_2p(x)\,dx$

微分熵与离散熵形式相似，但两者不能完全等同：

- 微分熵可以为负；
- 微分熵依赖变量的度量单位；
- 微分熵不直接表示“编码一个连续样本所需的比特数”。

因此，在连续变量场景中，互信息通常比微分熵更稳定、更容易解释。

---

# 熵、联合熵、条件熵与互信息

下面通过天气预测示例说明这些概念。

设：

- $X$：今天是否下雨；
- $Y$：早晨天空是否乌云密布。

假设边缘概率为：

$P(X=\text{下雨})=P(X=\text{不下雨})=0.5$

$P(Y=\text{乌云})=P(Y=\text{无云})=0.5$

那么，是否下雨的不确定性为：

$H(X)=-0.5\log_2 0.5-0.5\log_2 0.5=1\text{ bit}$

在观察天空之前，预测“今天是否下雨”平均有 `1 bit` 的不确定性。

## 联合熵 $H(X,Y)$

联合熵衡量同时观察两个随机变量时的不确定性：

$H(X,Y)=-\sum_x\sum_y p(x,y)\log_2p(x,y)$

假设 $X$ 和 $Y$ 的联合分布如下：

| $p(X,Y)$       | $Y=\text{乌云}$ | $Y=\text{无云}$ | 行和    |
|:-------------- |:-------------:|:-------------:|:-----:|
| $X=\text{下雨}$  | $0.4$         | $0.1$         | $0.5$ |
| $X=\text{不下雨}$ | $0.1$         | $0.4$         | $0.5$ |
| 列和             | $0.5$         | $0.5$         | $1.0$ |

> 按整行看 第一行 $p(X=下雨)$ ，第二列 表示早上观测到有乌云时今天下雨的概率$p(X=下雨,Y=乌云)=0.4$，第三列无云下雨的概率$p(X=下雨,Y=无云)=0.1$， 
> 
> 同理第二行 $p(X=不下雨)$， 第二列$p(X=不下雨,Y=乌云)=0.1$，第三列$p(X=不下雨,Y=无云)=0.4$
> 
> 早上乌云，大概率下雨；早上无云，大概率不下雨。但不是 100%



联合熵为：

$H(X,Y)=-\big[ \underbrace{0.4\log_20.4}_{下雨，乌云}+\underbrace{0.1\log_20.1}_{下雨，无云} + \underbrace{0.1\log_20.1}_{不下雨，乌云}+\underbrace{0.4\log_20.4}_{不下雨，无云}\big] \approx 1.722 bit$

---

## 条件熵 $H(X\mid Y)$

条件熵描述：已经知道 $Y$ 后，$X$ 还剩多少不确定性。

$H(X\mid Y)=H(X,Y)-H(Y)$

更完整的定义为：

$H(X\mid Y)=\sum_y p(y)H(X\mid Y=y)$

在本例中，由于：

$H(Y)=1\text{ bit}$

所以：

$H(X\mid Y)=1.722-1=0.722\text{ bit}$

这表示：知道早晨是否有乌云后，对于“今天是否下雨”，平均仍剩 `0.722 bit` 的不确定性。

也可以从条件概率理解：

$P(\text{下雨}\mid\text{乌云})=\frac{0.4}{0.5}=0.8$

$P(\text{下雨}\mid\text{无云})=\frac{0.1}{0.5}=0.2$

观察到乌云后，下雨的概率从 `0.5` 上升到 `0.8`；观察到无云后，下雨的概率下降到 `0.2`。因此，天空状况能够帮助预测是否下雨，但不能做到完全确定。

---

## 互信息 $I(X;Y)$

互信息衡量：知道一个随机变量后，另一个随机变量的不确定性减少了多少。

$I(X;Y)=H(X)-H(X\mid Y)$

等价地：

$I(X;Y)=H(X)+H(Y)-H(X,Y)$

本例中：

$I(X;Y)=1-0.722=0.278\text{ bit}$

这表示，观察“是否有乌云”平均能减少 `0.278 bit` 关于“是否下雨”的不确定性。

互信息具有以下性质：

$I(X;Y)=I(Y;X)\geq 0$

- 若 $X$ 与 $Y$ 独立，则 $I(X;Y)=0$；
- 互信息越大，说明两个变量之间的统计依赖越强；
- 互信息只能反映依赖关系，不必然表示因果关系。
  
  
  
  
