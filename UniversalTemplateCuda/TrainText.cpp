#include "pch.h"
#include "TrainText.h"

/* 
VectorInt64& TrainEncoded::GetAllData()
{
	if (dataList.size() == 0)
	{
		dataList.insert(dataList.end(), type.begin(), type.end());
		dataList.insert(dataList.end(), title.begin(), title.end());
		dataList.insert(dataList.end(), content.begin(), content.end());
	}

	return dataList;
}
*/