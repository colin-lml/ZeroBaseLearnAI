# 二次型

**定义：** 二次多项式是指这个多项式的项数超过1，且最高次方数为2的多项式。

**方程式：** $f(x_1,x_2)=x_1^2 + 2x_1x_2 + 3x_2^2$

**二次型矩阵(二次型通用形式)：** 

$\boxed{\boldsymbol{x}=\begin{bmatrix}x_1\\x_2\\ \vdots \end{bmatrix} ,\quad f(\boldsymbol{x}) = \dfrac{1}{2}\boldsymbol{x}^\top A \boldsymbol{x}}$ 

- $\boldsymbol{x}$是 $[n\times1]$ 向量

- $A$ 是 $[n\times n]$ 方阵

- 将方程式转换矩阵式目的是便于运算与理论推导（求导）
  
  

## 求$\nabla f(\boldsymbol x)$梯度推导：

$ \boxed{ \nabla f(\boldsymbol x)= \nabla\dfrac{1}{2}(\boldsymbol{x}^\top A \boldsymbol{x})=\begin{cases} A \boldsymbol x  & A==A^\top\\ \dfrac{1}{2}(A+A^{\top})\boldsymbol{x} & 通用式 \end{cases}}$



## 再次求导$\nabla^2 f(\boldsymbol x)$推导

$\nabla^2 f(\boldsymbol x)= \nabla \underbrace{\dfrac{1}{2}(A+A^{\top})\boldsymbol{x}}_{\nabla f(x)}=\dfrac{1}{2}(A+A^{\top})=\begin{cases} A \quad A=A^\top &海森矩阵\\ \dfrac{1}{2}(A+A^{\top}) & 通用式 \end{cases}$



## 求$A$方阵

方程式：$f(x_1,x_2)=x_1^2 + 2x_1x_2 + 3x_2^2$

二次型通用形式： 

$\boldsymbol{x}=\begin{bmatrix}x_1\\x_2\end{bmatrix} ,\quad f(\boldsymbol{x}) = \dfrac{1}{2}\boldsymbol{x}^\top A \boldsymbol{x}$

展开：

$f(\boldsymbol{x})=\dfrac{1}{2}\underbrace{\begin{bmatrix}x_1&x_2\end{bmatrix}}_{\boldsymbol{x}^\top} \quad  
\underbrace{\begin{bmatrix}a_{11} & a_{12}\\ a_{21} & a_{22} \end{bmatrix}}_{A} \quad
\underbrace{\begin{bmatrix}x_1\\x_2\end{bmatrix}}_{\boldsymbol{x}} = \dfrac{1}{2}
\underbrace{[a_{11}x_1^2+(a_{12}+a_{21})x_1x_2+a_{22}x_2^2]}_{方程式}$

$a_{11}=2,a_{22}=6,a_{12}+ a_{21}=4 $

$A= \begin{bmatrix} 2 & a_{12} \\ a_{21} &6 \end{bmatrix}$



# 海森矩阵 (Hessian Matrix)

**海森矩阵：** 梯度再对 $\boldsymbol{x}$ 求导（二阶偏导数矩阵）

- 连续性条件下: $\boldsymbol{H}$ 对称 ($H=H^\top$)

- 海森为常数矩阵也就是$\nabla^2 f(\boldsymbol x)=A=H$
  
  

**所以 $A$方阵：**

$A= \begin{bmatrix} 2 & 2 \\ 2 &6 \end{bmatrix}$



通过二次型公式求出$A$方阵，用矩阵运算代替求导过程

**代替求导：** $\nabla f(\boldsymbol x)=\nabla\dfrac{1}{2}(\boldsymbol{x}^\top A \boldsymbol{x})=A\boldsymbol x$

**海森矩阵：** $\nabla^2 f(\boldsymbol x)=A=H$



方程式：$f(x_1,x_2)=x_1^2 + 2x_1x_2 + 3x_2^2$ ，可以通过矩阵式进行求导

知道$A$矩阵带入求导公式：

$ \nabla f(\boldsymbol x)=A\boldsymbol x= \begin{bmatrix} 2& 2 \\ 2 &6 \end{bmatrix} \begin{bmatrix}x_1\\x_2\end{bmatrix}=\begin{bmatrix}2x_1+2x_2\\2x_1+6x_2\end{bmatrix}$

$ \nabla^2 f(\boldsymbol x)=A=H=\begin{bmatrix} 2 & 2 \\ 2 & 6 \end{bmatrix}$



# 泰勒展开

近似求解：在一个光滑函数上，选定某一点，构造多项式（方程式），在该点局部近似代替原本复杂的函数，用泰勒展开式作近似求解， 想要逼近效果更好，就要捕捉更多特征。

- **0 阶：** 只复制该点函数值，只有一个点，完全没有变化信息。

- **1 阶：** 加上**一阶导数（斜率）** → 切线近似（直线）

- **2 阶：** 再加**二阶导数（曲率）** → 抛物线，可以描述曲线怎么弯 （一般只作二阶）
  
  

设函数 $f(x)$ 在 $x_0$ 光滑，$\Delta x = x-x_0$



## 带皮亚诺余项

$\boxed{f(\Delta x +x_0)= \underbrace{\frac{f(x_0)}{!0}  }_{0阶}+ \underbrace{\frac{f'(x_0)\Delta x}{1!}}_{1阶} + \underbrace{\frac{f''(x_0)(\Delta x)^2 }{2!}}_{2阶}+  \underbrace{\frac{f'''(x_0)(\Delta x)^3 }{3!}}_{3阶} + \dots} $



## 二阶截断

- 一元函数：
  $\boxed{f(\Delta x +x_0) \approx \underbrace{f(x_0)}_{常数基准} + \underbrace{f'(x_0)\Delta x}_{切线、斜率信息} + \underbrace{\frac{1}{2} f''(x_0) (\Delta x)^2}_{曲率信息}}$
  
  

- 多元函数：

$\boxed{f(\Delta x +x_0) \approx f(x_0) + \nabla f(x_0)^{\top}(\Delta x) +\underbrace{\frac{1}{2}(\Delta x)^{\top} \nabla^2 f(x_0) \Delta x}_{\Delta x向量不能作平方运算,用 (\Delta x)^{\top} \times . \times \Delta x 运算}}$ 



**结合二次型公式用矩阵$A$代替求导** 

1. $\boxed{\nabla f(x_0){^\top} = (Ax_0)^{\top}=>x_0^{\top}A}$

2. $\boxed{\nabla^2 f(x_0)=A=H}$

**替换后**

$\boxed{f(\Delta x +x_0) \approx \underbrace{f(x_0)}_{x_0^{\top}Ax_0} + \underbrace{\nabla f(x_0)^{\top} \Delta x}_{x_0^{\top}A(\Delta x)} + \frac{1}{2}(\Delta x)^{\top} H \Delta x}$



**数据示例：**

方程式：$f(x_1,x_2)=x_1^2 + 2x_1x_2 + 3x_2^2$

$\textcircled{1}已知值 \begin{cases}f(\boldsymbol{x}) = \dfrac{1}{2}\boldsymbol{x}^\top A \boldsymbol{x} \\[4pt]
\\ A= \begin{bmatrix} 2 & 2 \\ 2 &6 \end{bmatrix}\\[4pt]
\\ 中心点: x_0=  \begin{bmatrix} 2  \\ 1  \end{bmatrix}\\[4pt]
\\增量: \Delta x=  \begin{bmatrix} 1  \\ 2  \end{bmatrix} \\[4pt]
\\目标: \ x= x_0+ \Delta x=\begin{bmatrix} 3  \\ 3  \end{bmatrix} 
\end{cases}$





$\textcircled{2}逐项计算 \begin{cases} f(x_0)=f(2,1)=\frac{1}{2} \underbrace{\begin{bmatrix} 2 & 1 \end{bmatrix}}_{x_0^{\top}} \underbrace{\begin{bmatrix} 2 & 2 \\ 2 &6 \end{bmatrix}}_{A} \underbrace{\begin{bmatrix} 2 \\ 1 \end{bmatrix}}_{x_0}=11\\[1pt]
\\ x_0^{\top}A(\Delta x)= \underbrace{\begin{bmatrix} 2 & 1 \end{bmatrix}}_{x_0^{\top}} \underbrace{\begin{bmatrix} 2 & 2 \\ 2 &6 \end{bmatrix}}_{A} \underbrace{\begin{bmatrix} 1 \\ 2 \end{bmatrix}}_{\Delta x}=26\\[4pt]
\\\frac{1}{2}(\Delta x)^{\top} A \Delta x=\frac{1}{2} \underbrace{\begin{bmatrix} 1 & 2 \end{bmatrix}}_{(\Delta x)^{\top}} \underbrace{\begin{bmatrix} 2 & 2 \\ 2 &6 \end{bmatrix}}_{A} \underbrace{\begin{bmatrix} 1 \\ 2 \end{bmatrix}}_{\Delta x}=17 \\[4pt]
\\f(\Delta x +x_0) \approx 11+26+17=54 \end{cases}$





$\textcircled{3}核验真值\begin{cases}二阶泰勒: f(\Delta x +x_0) \approx 11+26+17=54  \\
f(3,3)=3^2 + 2*3*3 + 3*3^2=9+18+27=54
\end{cases}$



# 牛顿法

**目标：** 求解无约束极小  $\min {f(\boldsymbol{x}) = \frac{1}{2} \boldsymbol{x}^\top A \boldsymbol{x}}$， $A=H$对称

**牛顿法核心思想** 当前位置 $\boldsymbol x_k$，**局部用二阶泰勒近似代替原函数**

**二阶泰勒：** $ f( \boldsymbol x_k + \boldsymbol d) \approx f(x_k) + \nabla f(x_k)^\top d + \frac{1}{2}d^{\top}Hd$

构造局部近似函数：$\phi(d) = f(x_k)+ \nabla f(x_k)^\top d + \frac12 d^{\top} H d$

- $\phi$ 函数记符号，指二阶泰勒近似函数

- $\phi(\boldsymbol d)$ ：从 $x_k$ 偏移 ${d}$ 后 使得$f(\boldsymbol x)$出现极小值 ，找出最优$\boldsymbol{d}^*$ 就是牛顿方向 $\boldsymbol{d}_k$

- 对 $\phi(\boldsymbol{d})$ 求导后令导数为$\nabla\phi(\boldsymbol d)=0$，得到极小值

$\phi(d) = \underbrace{f(x_k)}_{常量}+ \underbrace{\nabla f(x_k)^\top d}_{\nabla 项一} + \underbrace{\frac12 d^{\top} H d}_{\nabla 项二}$

**求导规则：**

- $\nabla_{\boldsymbol x} (\boldsymbol a^{\top} \boldsymbol x) = \boldsymbol a$ 其中$a$是向量

- $\nabla_{\boldsymbol x} (\boldsymbol x^{\top}\boldsymbol a) = \boldsymbol a$ 其中$a$是向量

- $\nabla_{\boldsymbol x}(\boldsymbol x^{\top} A \boldsymbol x) = \boldsymbol{A x} + A^{\top} \boldsymbol x$ 其中$x$是向量，$A$是矩阵
  
  

$\nabla$ 项一 ：$\nabla f(x_k)=g_k$是常数行向量  所以 $\nabla_d (g_k^{\top} d)=g_k$

$\nabla$ 项二: $\nabla_d(\frac12 d^{\top} H d)=Hd$



对 $\phi(\boldsymbol{d})$ 求导结果：$\nabla_d \phi(d)=0 +g_k+Hd$ ，求极小值$\nabla \phi(\boldsymbol{d})=0$置零$g_k+Hd=0$

$Hd=-g_k$ ，左右同时乘以 $H^{-1}$

**通用式：**

 $\boxed{d=-H^{-1}g_k \quad g_k=\nabla f(x_k)=Hx_k}$



**如何求$H^{-1}$矩阵**

$H=\begin{bmatrix}a& b\\ c&d\end{bmatrix} \Rightarrow H^{-1}=\dfrac{1}{ad-bc} \begin{bmatrix}d & -b\\ -c&a\end{bmatrix}$

行列式:$det(H)=ad-bc$

记忆口诀：主对角线互换 $a\leftrightarrow d$，副对角线变号 $-b,-c$，整体除以行列式。



**数据示例：**

$x_k=\begin{bmatrix} 1 \\ 1 \end{bmatrix}$

$H= \begin{bmatrix} 2 & 2 \\ 2 &6 \end{bmatrix} \quad H^{-1}=\dfrac{1}{2*6-2*2} \begin{bmatrix} 6 & -2 \\ -2 &2\end{bmatrix}=\dfrac18\begin{bmatrix} 6 & -2 \\ -2 &2 \end{bmatrix}=\begin{bmatrix} \dfrac34 & -\dfrac14 \\ -\dfrac14 &\dfrac14 \end{bmatrix}$



引用公式 $\boxed{d=-H^{-1}g_k \quad g_k=\nabla f(x_k)=Hx_k}$

$g_k=Hx_k=\begin{bmatrix} 2 & 2 \\ 2 &6 \end{bmatrix} \begin{bmatrix} 1 \\ 1 \end{bmatrix}=\begin{bmatrix} 4 \\ 8 \end{bmatrix}$

$d=-H^{-1}g_k=-\begin{bmatrix} \dfrac34 & -\dfrac14 \\ -\dfrac14 &\dfrac14 \end{bmatrix} \begin{bmatrix} 4 \\ 8 \end{bmatrix}=-\begin{bmatrix} 1 \\ 1 \end{bmatrix}$

**注意不能合并**  $d=-H^{-1}g_k=> -H^{-1} Hx_k=-Ix_k=-x_k$



**关键结论**

1. 任意正定二次函数 $\displaystyle f=\frac12\boldsymbol x^\top H\boldsymbol x$

2. 牛顿方向 $\boldsymbol d_k=-H^{-1}g_k$

3. 步长$\alpha=1$，**单次迭代直达全局最优**
   
   

# 最速下降法

## 柯西 - 施瓦茨不等式

对任意两个同维度实向量 $\boldsymbol{a},\boldsymbol{b}$：

$|a^\top b| \le \|a\| \cdot \|b\|$ 其中$|a^\top b|$是向量内积结果的绝对值 ,$\|a\|、\|b\|$是向量的模

- 如 $|a^\top b| = \|a\| \cdot \|b\|$ 说明 $a、b$ 共线
  
  

## 最速下降法

**目标：** 求解无约束极小 $\begin{cases} \min {f(\boldsymbol{x}) = \frac{1}{2} \boldsymbol{x}^\top A \boldsymbol{x}} & A=H对称 
\\H=\begin{bmatrix} 2 & 2 \\ 2 & 6 \end{bmatrix} \end{cases}$

**牛顿法核心思想** 只使用一阶泰勒近似（只利用梯度，无曲率）:

$ \phi(d) =f(\boldsymbol x_k + \boldsymbol d) \approx \underbrace{f(x_k)}_{常量} + \underbrace{g_k^\top}_{g_k = \nabla f(x_0)} d $

因为$\phi(d)={g_k^\top}d$是线性函数不能求导，我们只能限定：**只允许单位长度方向**，只优化「方向」，不能无限走远。

$\min_{||d||=1} \phi(d)=g_k^{\top}d$ 根据 **柯西 - 施瓦不等式共线原理** $|g_k^{\top}d|=\|g_k\|\cdot\|d\| ,\quad \|d\|=1$

- 负梯度方向 = 最速下降方向

- 正梯度方向=上升最快的方向
  
  

**单位向量下 最速下降方向:**

$d=-\dfrac{g_k}{\|g_k\|}$



## 线性搜索（Line Search）

## 精确搜索

$\textcircled{1}$求$\alpha$ 是步长

$g_k=\nabla f(x_k)=Hx_k$

$\phi(\alpha)=f(x_k-\alpha g_k)=\frac12(x_k-\alpha g_k)^\top H (x_k-\alpha g_k)=\frac12[\underbrace{(x_k^\top-\alpha g_k^\top) H}_{看成一个整体} (x_k-\alpha g_k)]$

$=>(x_k^\top-\alpha g_k^\top) H(x_k-\alpha g_k)=(x_k^\top-\alpha g_k^\top) H x_k-(x_k^\top-\alpha g_k^\top) H ag_k$

$=>\frac12[x_k^\top H x_k-\underbrace{\alpha g_k^\top Hx_k - \alpha x_k^\top H g_k}_{-2\alpha x_k^\top Hg_k} + \alpha^2g_k^\top Hg_k]$

$=>\phi(\alpha) =\dfrac12x_k^\top Hx_k -\alpha x_k^\top Hg_k + \dfrac12 \alpha^2 g_k^\top H g_k$

$\nabla \phi(\alpha)=-x_k^\top Hg_k+ \alpha g_k^\top H g_k=0$

$\quad$

$\alpha=\dfrac{x_k^\top Hg_k}{g_k^\top Hg_k }=\dfrac{g_k^\top \overbrace {Hx_k}^{g_k=Hx_k} }{g_k^\top Hg_k }=\dfrac{g_k^\top g_k}{g_k^\top Hg_k }$


